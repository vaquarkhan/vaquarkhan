// Simple ArchitectureModal stub to prevent import errors
export const architectureManager = {
    showArchitectureModal: function(storyId) {
        console.log('ArchitectureModal: Opening for story:', storyId);
        // This is handled by main.ts now
        return true;
    }
};

console.log('ArchitectureModal loaded');